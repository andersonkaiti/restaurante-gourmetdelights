window.addEventListener("load", () => {
    document.querySelectorAll(".content-animation").forEach(elemento => {
        elemento.style.opacity = "1";
        elemento.style.marginTop = "0";
    }); 
});
